---
interface User {
	name: {
		first: string
		last: string
	}
}

const response = await fetch('https://randomuser.me/api/')
const data = (await response.json()) as { results: User[] }
const randomUser = data.results[0]
---

<p>User - {randomUser.name.first} {randomUser.name.last}</p>
